registry=https://registry.npmjs.org/
fetch-retries=10
fetch-retry-mintimeout=30000
fetch-retry-maxtimeout=300000
strict-ssl=true
# Reduce concurrent network requests in CI to be more gentle on registry
maxsockets=8
# Reduce pnpm network concurrency as well
network-concurrency=4
